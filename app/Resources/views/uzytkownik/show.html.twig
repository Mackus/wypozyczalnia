{% extends 'base.html.twig' %}

{% block body %}
    <h1>Uzytkownik</h1>

    <table>
        <tbody>
            <tr>
                <th>Saldo</th>
                <td>{{ uzytkownik.saldo }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ uzytkownik.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('uzytkownicy_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('uzytkownicy_edit', { 'id': uzytkownik.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
